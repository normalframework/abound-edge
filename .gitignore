/dist
*.js.map
node_modules
.npm
combined.log
error.log
.DS_Store

# config/: ignore live settings (secrets), user overrides, runtime caches,
# previews and generated artifacts. Keep only the shipped defaults + template.
config/*
!config/default-mapping.json
!config/settings.example.json
